<!--
Shared IDE-page prerequisites.

The visible example intentionally repeats these imports so students can see a
complete runnable example. This hidden cell makes newly added code blocks ready
to use immediately after the page has loaded or the Python environment restarts.
-->

```{pyodide-python}
#| context: setup

import numpy as np
import matplotlib.pyplot as plt
```
