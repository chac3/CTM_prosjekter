---
title: "Prosjekt for IMAx2022/IMAx2023/IMAx2024"
format:
  html:
    toc: true
    code-copy: true
jupyter: false
execute:
  enabled: false
---

<!-- Generated by ipynb_to_quarto.py. Review before publication. -->
<!-- Source SHA-256: 425c5609b68427abb7cd1eb736e89a0ed440758de3e6938d3864128d02f522b1 -->

# Prosjekt for IMAx2022/IMAx2023/IMAx2024

## Introduksjon til prosjektet

*Les nøye gjennom denne teksten før dere tar fatt på arbeidet*.
*Legg merke til at små endringer i prosjektbeskrivelsen kan forekomme. Siste versjon av prosjektbeskrivelsen og notatene finner dere bestandig på Jupyter-huben.*

Dere skal løse oppgavene i teksten under, og levere en rapport som beskriver løsningen. Litt praktisk informasjon:

- Innleveringsfrist: 20. mars, klokken 12:00.
- Lengde: *Maksimalt* 5000 ord. (Kildekode kommer i tillegg.)
- Innleveringsformat: *Én* PDF-fil. 
- Innleveringssted: Inspera.

Målet med dette prosjektet er at dere får erfaring med å:

- jobbe med partielle differensiallikninger
- søke opp informasjon
- samarbeide i grupper
- skrive en rapport

Karakteren gis etter prosentvurderingsmetoden https://i.ntnu.no/wiki/-/wiki/norsk/prosentvurderingsmetoden. Dere får 0-100 poeng, hvorav 30 poeng for en generell vurdering av kvaliteten på rapporten og 70 poeng for løsning av de ulike oppgavene.

Forprosjektet skal gjennomføres med frist 13. februar, og *må* gjennomføres for å få prosjektet godkjent.

## Rapport (30 poeng)

Språket skal være formelt, saklig og korrekt. Vi kommer til å vurdere om matematisk notasjon og matematiske begrep er riktig brukt i forklaringene. Når dere skriver rapporten, så kan dere anta at dere skriver til en medstudent i klassen som dere ikke kjenner så godt.

Det er bedre å forklare noe kort, presist og riktig i stedet for å bruke lange upresise utredninger. 

Det er dere som skal øve på å skrive rapport, så ikke kopier teksten fra andre kilder. Dette inkluderer tekst fra andre studenter, nettsider, bøker og bruk av KI. Kopiering blir vurdert som juks. *Skriv selv og med egne ord*.

Det skal gå klart frem av teksten hvordan dere kom frem til alle svar. Den som leser teksten skal lett kunne etterprøve om alt er riktig gjennomført, og om det dere skriver er sant. Hvis svaret på oppgaven krever utregninger for hånd, så skal mellomregninger være med.

Der dere har programmert, så må kildekode inkluderes. Kildekode skal være fornuftig kommentert. Kildekoden teller ikke på antall ord.

Hvis dere har brukt andre kilder eller kilder eller hjelpemidler enn diskusjon innad gruppa, forelesninger eller prat med lærerne, så må dere oppgi disse kildene/hjelpemidlene i svaret på hver oppgave. Det skal gå klart frem av oppgaven akkurat hva dere har brukt.

Merk! Det er *ikke* et krav at dere skal bruke eksterne hjelpemidler.

### Hjelpemidler

Den viktigste informasjonen dere trenger kommer fra matematikkfagene fra høst og vår, og ved å diskutere problemstillingene med alle de andre i gruppa. 

I løpet av prosjektet *så* kan dere også bruke andre hjelpemidler, som å

- søke på nettet etter relevant stoff.
- bruke KI til å hjelpe dere med å lære stoffet (men ikke til å skrive rapporten).
- spørre fagpersoner (studentassistenter, stipendiater eller faglærere).

Siden KI ikke kan etterprøves så må dere i tillegg finne en kilde som er mer pålitelig, eller på en annen måte forsikre dere om at informasjonen er korrekt. 

Merk! *Det er ikke lov å bruke KI til å skrive rapporten. Det vil bli vurdert som juks. Skriv rapporten med egne ord*.

Merk! *Det er ikke lov å bruke løsningen til andre grupper i klassen. Det vil bli vurdert som juks.*

Det er ikke et krav at dere skal bruke eksterne hjelpemidler, men hvis det er nødvendig så må dere henvise til disse i teksten i rapporten.

### Kildehenvisning

Det skal gå klart frem på hver oppgave hvilke kilder dere har brukt i løsningen av oppgaven.

Den viktigste kilden dere har er informasjon fra forelesningene. For de fleste oppgavene så vil forelesningene, i tillegg til diskusjon i gruppa, være nok til å løse oppgaven. 

Men det kan være at dere må bruke andre kilder til informasjon. I så fall så er det noen ting du må passe på.

Tommelfingerregelen du skal bruke er at alle påstander som dere kommer med, og som ikke er direkte vist av dere i prosjektet, skal refereres til. Det viktigste formålet med kilder er å gi leseren muligheten til å sjekke at informasjonen stemmer og hvor den kommer fra. Derfor er det en fordel å velge troverdige kilder. 

Store språkmodeller, som ChatGPT, gir forskjellige svar basert på samme spørsmål og er derfor ikke en pålitelig kilde. Det er også en risiko for at disse gir feil informasjon. Bruk heller kildene som språkmodellene henviser til, og finn ut hvor informasjonen står i kilden dere refererer til. 

Generelt oppfordrer vi til at dere bruker sunn fornuft når det kommer til kilder, og at dere prøver å bruke konvensjonene som er vanlig innenfor fagområdet dere studerer. For mer om kildebruk, se https://i.ntnu.no/oppgaveskriving/bruke-og-referere-til-kilder.


### Tips for skriving av rapport

Her er noen tips som vil gjøre skriving av rapporten *mye* enklere.

For hver oppgave:

- Bestem hvem som er ansvarlig for å skrive i rapporten for akkurat denne oppgaven. Bestem dette før dere starter med oppgaven.
- Før en stikkordslogg, og skriv ned det dere gjør underveis mens dere jobber med oppgaven. 
- Noter ned de kildene dere bruker mens dere lærer.
- Når en oppgave er gjennomført og alle spørsmål er besvart, skriv ferdig den delen av rapporten umiddelbart.
- La alle i gruppa lese gjennom svaret på hver oppgave, og komme med tilbakemeldinger på teksten.
- Gå videre til neste oppgave.

Pass på at rapporten svarer på alle spørsmålene i samme rekkefølge som oppgitt her.

Se nøye gjennom malen vi gir dere her.


### Innlevering av rapport

Rapporten skal leveres i Inspera.

Dere skal enten bruke det som kalles *kandidatstyrte grupper uten passordbeskyttelse* eller *kandidatstyrte grupper med passordbeskyttelse* for å levere arbeidet.

I god tid før fristen må dere gjøre følgende:

- Ett gruppemedlem logger inn i Inspera, trykker på prøven, og deretter *lag ny gruppe*. Den som lager gruppen må også lage en PIN-kode.
-Resten av medlemmene i gruppa logger inn i Inspera én etter én, trykker på prøven, og deretter *bli med i gruppe*. For å bli med i gruppen bruker dere PIN-koden som ble valgt i steg 1. Den siste som blir med i gruppen bør sjekke at antall gruppemedlemmer er riktig. 
- Når alle på gruppa har meldt seg inn, velger dere en person som har ansvar for å laste opp prosjektarbeidet. Kun en person kan åpne prøven i Inspera om gangen. De filene som er lastet opp før fristen blir automatisk levert når fristen går ut.
-Etter fristen er ute bør alle sjekke at rett fil ligger under *Arkiv* i Inspera.

Dere kan finne mer informasjon om hvordan levere inn her: https://i.ntnu.no/wiki/-/wiki/Norsk/Gruppeinnlevering+i+Inspera+-+for+studenter

# Forprosjekt: 

Prosjektet skal gjøres som et gruppearbeid med 3-6 personer per gruppe.

Vi ønsker at dere finner dere en gruppe på egenhånd. Hvis dere hadde en gruppe i matematikk 1 som fungerte 
greit så kan dere fortsette å jobbe i den gruppen. 

Det skal være 3-6 medlemmer per gruppe.

(Vi kan i nødstilfeller hjelpe dere med å finne en gruppe. Følg med på Blackboard for 
informasjon om prosessen på din campus.)

1. Når dere har funnet dere en gruppe å jobbe sammen med, så vil vi at dere i felleskap skriver en gruppekontrakt.  Malen for gruppekontrakt finner dere her.

2. Meld dere deretter samtidig inn på en gruppe på blackboard. Pass på at gruppens størrelse passer med antall medlemmer. Innmelding må skje samtidig for alle gruppemedlemmer.

3. Lever gruppekontrakten på blackboard før 13. februar.

4. I siste oppgave av prosjektrapporten skal dere diskutere hvordan samarbeidet har fungert. Diskuter eventuelle avvik der.

Forprosjektet er obligatorisk og må gjøres for å få prosjektet godkjent.

# Hovedprosjekt:

Hovedprosjektet består av to deler. I del 1 skal dere simulere trafikkflyten på en veistrekning, og i del 2 skal dere simulere oppvarmingen av en gjenstand. Modellene i simuleringene vil være basert på partielle differensiallikninger, som vi skal utforske, og løse numerisk.

## Del 1: Trafikkmodell: tetthet, hastighet og fluks

Vi skal modellere trafikkflyten langs en veistrekning. Vi innfører flere forenklinger:

- det er ingen veikryss 
- ingen forbikjøringer
- kun én kjørefil
- ingen kjører ut av eller inn på veien
- alle bilene er like lange
- trafikken flyter én vei
- alle sjåførene har samme kjøreadferd, som inkluderer at alle holder seg til fartsgrensen.
- m. m.

Merk at det er mulig å modellere trafikken med et system av ordinære differensiallikninger hvor vi regner på posisjon, hastighet og akselerasjon til hver enkelt bil. Det ville vært en såkalt *mikroskopisk* modell.

Det er derimot *ikke* hva vi skal gjøre i denne oppgaven. Vi skal se på en *makroskopisk* modell.

Vi modellerer trafikken som den ser ut observert fra siden av veien. Det observeres altfor mange biler til at vi kan se på hver enkelt bil. Så vi beskriver trafikken ved hjelp av funksjonen $u(x,t)$, som gir tettheten av biler i posisjon $x$ langs veien ved tidspunkt $t$. Vi skal videre studere en veistrekning $x\in[a,b]$.

Vi tenker oss at $u(x,t)$ for et tidspunkt $t$ er gitt ved antall biler på strekning $[x,x+R]$ delt på lengden av strekningen $R$, hvor $R$ er en passende lengde.

Måleenhet for $u(x,t)$ er derfor antall biler per meter. Vi har alltid $u(x,t)\geq 0.$

Hastigheten $v(x,t)$ til bilene ved posisjon $x$ og tidspunkt $t$ er også viktig.

#### Vår viktigste antakelse er at hastigheten $v(x,t)$ kun er avhengig av tettheten $u(x,t)$. Dvs.  hastigheten $v=v(u)$ er en funksjon av $u$.

For hastigheten $v$ så antar vi i tillegg at:

1. $v\geq 0$  
2. $v\leq v_{max}$, hvor $v_{max}$ er maksfart, dvs. fartsgrensen på veien.
3. $\frac{dv}{du}\leq 0$.
4. $v(0) = v_{max}$ når tettheten $u=0$.
5. $v(u_{max}) = 0$ når tettheten $u=u_{max}$ er størst mulig. 

Det er mange mulige valg av funksjoner $v=v(u)$. I praksis er dette noe vi må måle på den aktuelle veistrekningen. Det blir derimot for komplisert og tidkrevende for oss. Istedet prøver vi med noen funksjoner som kan passe.

Den enklest mulige funksjonen er den lineære funksjonen 
$$
\begin{align}\tag{1}
  v=v_{max}\left(1-\frac{u}{u_{max}}\right).
  \end{align}
$$

En annen mulig funksjon er den kvadratiske funksjonen
$$
\begin{align}\tag{2}
  v=v_{max}\left(1-\left(\frac{u}{u_{max}}\right)^2\right).
  \end{align}
$$

Dette er på ingen måte de eneste mulighetene for $v(u)$. Vi gir flere eksempler lenger nede i oppgaven, og vi vil også at dere skal prøve å lage noen slike funksjoner.

I oppgaven kommer vi hovedsaklig til å anta at funksjonen $v(u)$ er kjent. Hvis vi hadde brukt modellen i virkeligheten så måtte vi ha gått ut og målt hva $v$ er som funksjon av tettheten $u$. Og da ville lengden $R$ ha spillt en viktig rolle. Men siden vi bare antar hva $v(u)$ er, så spiller ikke lengden $R$ noen stor rolle.

Vi skal også se på strømmen eller *fluksen* av biler gjennom et punkt $x$ langs veien ved tiden $t$. 

Vi definerer fluksen av biler, $J(x,t)$, til å være antall biler som passerer et punkt $x$ per sekund. Altså har $J$ måleenhet antall biler per sekund. I vår modell antar vi følgende sammenheng mellom tettheten $u$, hastigheten $v$ og fluksen $J$:
$$
\begin{align}\tag{3}
  J(x,t)=J(u(x,t))=u(x,t)v(u(x,t)).
\end{align}
$$
og vi krever at $J(x,t)\geq 0$.

Antall biler som passerer punktet $x$ på veien i tidsintervallet $[t,t+\Delta t]$ er altså (tilnærmet lik)
$$
\begin{align}\tag{4}
  J(x,t)\Delta t
  \end{align}
$$

Videre er antall biler som befinner seg på strekningen $[x,x+\Delta x]$ ved tiden $t$ gitt ved (tilnærmet) 
$$
\begin{align}\tag{5}
  u(x,t)\Delta x
\end{align}
$$

Nå skal vi sette opp et regnskap for bilene, som er helt analogt med det de fleste av oss har et forhold til. Vi vet at penger inn på konto minus penger ut av konto over et tidsrom er lik netto endring av saldoen.

Tilsvarende er antall biler på strekningen $[x,x+\Delta x]$ ved tiden $t+\Delta t$ minus antall biler på strekningen $[x,x+\Delta x]$ ved tiden $t$ lik netto endring i antall biler på strekningen $[x,x+\Delta x]$ i tidsrommet $t$ til $t+\Delta t$.

På samme måte som med bankkontoen, må dette være lik antall biler som kjører inn på strekningen $[x,x+\Delta x]$ minus antall biler som kjører ut fra strekningen $[x,x+\Delta x]$ i tidsrommet fra $t$ til $t+\Delta t$.

Uttrykker vi regnskapet matematisk, så fører det til ligningen
$$
\begin{align}\tag{6}
    u(x,t+\Delta t)\cdot \Delta x-u(x,t)\cdot \Delta x=-J(x+\Delta x, t)\cdot \Delta t + J(x,t)\cdot \Delta t.
\end{align}
$$
som etter deling på $\Delta x$ og $\Delta t$ gir
$$
\begin{align}\tag{7}
    \frac{u(x,t+\Delta t)-u(x,t)}{\Delta t}=-\frac{J(x+\Delta x, t)-J(x,t)}{\Delta x}.
    \end{align}
$$
Som vanlig i denne typen modellering ser vi på ligningen over når $\Delta x\rightarrow 0$ og $\Delta t\rightarrow 0$. 

Ligningen over blir da den partielle differensiallikningen
$$
\begin{align}\tag{8}
    \frac{\partial u(x,t)}{\partial t}=-\frac{\partial J(x,t)}{\partial x}
    \end{align}
$$
som gjelder for verdier $x\in [a,b]$ og $t\geq 0$.  

Som vi har sett ovenfor, er $J$ er en funksjon av $u$ gitt ved $J(u)=u\cdot v(u)$, slik at likningen til slutt kan skrives som 
$$
\begin{align}\tag{9}
  u_t + (u\cdot v(u))_x = 0
\end{align}
$$

I tillegg så vil en fullstendig problembeskrivelse inneholde mer informasjon. For eksempel så beskriver randbetingelsen $u(a,t)=u_a(t)$ hvor stor tettheten av biler er til enhver tid ved startpunktet av veien $(a)$, mens initialbetingelsen $u(x,0)=u_0(x)$ beskriver tettheten av biler på alle posisjoner ved starttid $t=0$.

## Oppgave 1. (10 poeng)

### a) Funksjonen $u(x,t)$.

i) Hvis vi velger og holder fast et tidspunkt $t=t_0$ og ser på grafen til envariabelfunksjonen $u(x,t_0)$, hva er det grafen beskriver?

ii) Hvis vi velger og holder fast en posisjon $x=x_0$ og ser på grafen til envariabelfunksjonen $u(x_0, t)$, hva er det grafen beskriver?

iii) Tenk igjennom og forklar hva som skjer med funksjonen $u(x,t)$, hvis vi endrer lengden på strekningen $R$ (som vi brukte i definisjonen av $u(x,t)$), fra å være liten (f.eks. en billengde) til å bli stor (f.eks. $100$ billengder).

### b) Funksjonen $v=v(u)$

i) Forklar hvorfor $v(0)=v_{max}$, $v(u_{max})=0$ og $\frac{dv}{du}\leq 0$ er rimelige antakelser. 

ii) Regn og vis at $v(u)=v_{max}\left(1-\left(\frac{u}{u_{max}}\right)^p\right)$ for alle verdier $p\geq 1$ tilfredsstiller de fem antakelsene vi har listet opp ovenfor for hastighetsfunksjonen.

iii) Lag to andre veldig ulike funksjoner $v(u)$ som tilfredsstiller antakelsene. Skriv ned funksjonene med en matematisk riktig formel, og tegn grafen til funksjonen ved hjelp av python. Dere må vise at funksjonene tilfredsstiller antakelsene.

(Hint: kanskje du kan bruke trigonometriske eller logaritmiske funksjoner.)

### c) De deriverte

i) Hva er måleenhetene til $\frac{dv}{du}$, $\frac{\partial u}{\partial t}$, $\frac{\partial u}{\partial x}$, $\frac{\partial v}{\partial t}$ og $\frac{\partial v}{\partial x}$?

ii) Forklar hva hver av disse deriverte forteller oss om trafikkflyten. 

(Hint: $\frac{\partial v}{\partial t}$ er *ikke* akselerasjonen til bilene)

### d) Utregning av fluksen

Forklar hvorfor fluksen er gitt ved
$$
\begin{align}\tag{10}
    J(x,t)=J(u(x,t))=u(x,t)v(u(x,t)),
\end{align}
$$
der $v(u)$ er hastigheten til bilene når biltettheten er $u$. Det vil si at fluksen $J=J(u)$
også er en funksjon av tettheten $u$.

Prøv å gjøre forklaringen deres så presis og matematisk som mulig. Vis regning hvis det er nødvendig.


### e) Maksimal fluks 

Fluksen er maksimal i et punkt $x$ når det strømmer så mange biler som mulig gjennom $x$. 

i) Hvis hastighetsfunksjonen er $v(u)=v_{max}\left(1-\frac{u}{u_{max}}\right)$, hva er hastigheten når strømmen (fluksen) er maksimal? Hva er den maksimale fluksen?

ii) Regn ut hastigheten ved maksimal fluks for hastighetsfunksjonen $v(u)=v_{max}\left(1-\left(\frac{u}{u_{max}}\right)^p\right)$.  


### f) Differensiallikningen

Sjekk ved regning at begge sider av likningene 
$$
\begin{align}\tag{11}
    \frac{\partial u(x,t)}{\partial t}=-\frac{\partial J(x,t)}{\partial x}.
\end{align}
$$
har samme måleenhet. Hva er måleenheten?

## Oppgave 2. Rødt lys (15 poeng)

Vi ser på trafikkflyten på en veistrekning med posisjoner $x\in [-1000,1000]$ og tid $t\geq 0$.
Måleenhetene er meter og sekunder. 

Når $t<0$ så står alle bilene og venter ved et rødt lys ved posisjon $x=0$. Det er ingen biler ved $x>0$, og bilene står med tetthet $u_{max}$ for $x<0$. 

Når $t=0$ så blir lyset grønt, og bilene begynner å bevege seg.

Modellen beskrives (som forklart over) med:
$$
\begin{align}\tag{12}
u_t + J(u)_x = 0, \quad J(u) = u\cdot v(u)
\end{align}
$$
for $x\in [-1000,1000]$ og $t\geq 0$.

Vi bruker 
$$
\begin{align}\tag{13}
v(u) = v_{max} \left(1-\left(\frac{u}{u_{max}}\right)^p\right)
\end{align}
$$
for et tall $p\geq 1$. 

Vi antar at det ikke kommer noen nye biler inn i veistrekningen. Dvs. $u(-1000,t)=0$ for alle
verdier av $t$.

### a) Initialverdibetingelsen

Vi har initialverdibetingelsen $u(x,0)=u_0(x)$ som beskriver tettheten av biler mens det er rødt lys.

Skriv ned en formel som beskriver $u_0(x)$, og tegn en skisse til denne funksjonen.

Diskuter grafen, og forklar hvorfor grafen ser ut slik den gjør.

### b) Verdiene $v_{max}$ og $u_{max}$ og randbetingelsen for $x=1000$.

Dere må selv velge noen passende (og realistiske) verdier for verdiene $v_{max}$ og $u_{max}$ og forklare hvorfor dere mener dette er gode verdier. Dere kan ta utgangspunkt i vanlige biler på vanlige norske veier.

Vi trenger også en randbetingelse $u(1000, t)$. Forklar hva det betyr for trafikken på veien for $x>1000$ hvis vi antar $u(1000,t)=0$? Hva sier det om trafikken for $x>1000$ hvis $u(1000,t)=u_{max}$?

Dere skal bruke randbetingelsen $u(1000, t)=0$ i fortsettelsen.

### c) Løs differensiallikningen

Løs likning $[12]$ ved hjelp av Lax-Friedrichs sin metode for de tre ulike verdiene $p=1,2,5$. Du finner flere eksempler på Lax-Friedrichs metode i notatbok 3 om <i>Transportligninger</i>.

Lag animasjoner som viser hvordan $u(x,t)$ endrer seg med hensyn på tiden. Beskriv i rapporten hva som skjer. (Du trenger ikke ta med animasjonene i rapporten).

Svar deretter på d) og e) for hver av de tre verdiene av $p$.

### d) Bevegelsen til en enkelt bil

La $V(t)$ og $P(t)$ beskrive hastigheten og posisjonen til en bil som befinner seg ved $x=-1000$ på veien. 
Regn ut $V(t)$ og $P(t)$, og tegn grafen til disse. 

For å løse problemet så må du bruke at:

1. $P(0)=-1000$ og $V(0)=0$ 
2. $V(t)=v(u(P(t), t))$
3. $P(t + \Delta t) = P(t) + \Delta t\cdot V(t)$

### e) Hastigheten til en kø

La $K(t)$ være posisjonen til bilen som starter å bevege seg ved tid $t$.

Finn $K(t)$, og tegn grafen.

Hva er tiden $t$ når alle bilene er i bevegelse?

### f) Valg av parameter $p$

Med utgangspunkt i svarene fra c) og d) og Oppgave 1 f), forklar hvordan ulike valg av $p\geq 1$ beskriver ulike valg for kjøreatferd i trafikkmodellen.

## Del 2: Varmeledning

Varmeledning er prosessen der varme overføres gjennom et materiale. Dette skjer når det er temperaturforskjeller i ulike deler av materialet. For å beregne varmeledning, så kan man løse varmelikningen.

Vi skal etter hvert løse varmelikningen, hvor vi ser på en modell for steking i en ovn. 

Før vi kommer så langt så varmer vi opp med noen øvingsoppgaver om løsning av (relevante) partielle differensialligninger. Vi henviser til forelesningene og jupyter-notatene for informasjon om hvordan likningene løses.

## Oppgave 3: Poissonligning, 1D (5 poeng)

Vi begynner med å se på ligningen
$$
\begin{align}\tag{14}
u_{xx}(x) = f(x), \quad -1\leq x\leq 1
\end{align}
$$
hvor $f(x)=\cos(\pi x)$.

### a)

Løs ligningen analytisk for (Dirichlet-)randbetingelsene $u(-1) = 0$ og $u(1) = 2$.

### b)

Finn en numerisk løsning for likningen i a). 

Tegn grafen til løsningen sammen med løsningen fra a). Er løsningene like?

## Oppgave 4: Varmeligning, 1D (5 poeng)

Vi løser nå *varmeligningen*

$$
\begin{align}\tag{15}
u_t(x,t) = u_{xx}(x,t) - f(x), \quad t\geq 0, -1<x<1
\end{align}
$$
hvor $f(x)=\cos(\pi x)$.

Vi bruker initialbetingelsene
$$
\begin{align}\tag{16}
u(x,0) = 1 + x + 5\sin(\pi x).
\end{align}
$$

Løs ligningen numerisk for (Dirichlet-)randbetingelsene $u(-1,t)=0$ og $u(1,t)=2$.

Bruk forlengs Euler med et passende tidssteg.

## Oppgave 5: Poissonligning, 2D (5 poeng)

Nå skal vi se på numerisk løsning av Poissonligningen i 2 dimensjoner

$$
\begin{align}\tag{17}
u_{xx} + u_{yy} = f(x,y), \quad -5<x<5, 0<y<2.
\end{align}
$$

Vi velger $f=0$ og følgende randbetingelser:

$$
\begin{align}\tag{18}
u(-5,y) = \sin(2\pi y), \quad & 0\leq y \leq 2 \\
u(x,0) = 0, \quad & -5\leq x \leq 5 \\
u(5,y) = \sin(2\pi y), \quad & 0\leq y \leq 2 \\
u(x,2) = \sin(\pi x), \quad & -5 \leq x \leq 5
\end{align}
$$

Løs ligningen numerisk.

## Oppgave 6: Varmeligning,  (10 poeng)

Nå skal vi modellere steking av et legeme i en ovn. Vi ser på et tverrsnitt av legemet, slik at vi kun trenger de to koordinatretningene $x$ og $y$, og ikke dybderetningen $z$ for ovnen og legemet. 

Den partielle differensiallikningen er lik
$$
\begin{align}\tag{19}
u_t = \alpha\cdot(u_{xx} + u_{yy}),
\end{align}
$$
hvor $\alpha$ er legemets *termiske diffusivitet*. Passende tall for $\alpha$ kan slås opp på nett.

Vi antar at temperaturen av luft i ovnen holdes konstant på $200$ grader Celsius hele tiden, og bruker altså randbetingelsene at $u(x,y)=200$ på alle kantene av legemet.

Differensialligningen er kun definert for legemet, vi tar ikke med ovnen rundt i denne oppgaven.

Ved tiden $t=0$ (når legemet settes i ovnen) så er temperaturen lik $15$ grader i hele legemet.

### a)

Velg en gjenstand å ta utgangspunkt i, hvor gjendstanden skal ha et rektangulært tverrsnitt. Hvilket materiale har gjenstanden, og hva er den termiske diffusitiviteten $\alpha$?

Bestem deretter hvilke verdier for koordinatene $x$ og $y$ som beskriver gjenstanden.

### b)

Sett opp den partielle differensiallikningen med rand- og initialbetingelser. Løs deretter likningen numerisk.

Legg ved *varmeplot* (altså fargeplot med rødt for varmt og blått for kaldt) for noen ulike tidspunkt.

### c)

Hvor lang tid tar det før temperaturen i midten av legemet når 60 grader? Legg ved et varmeplot for tidspunktet dette skjer.

### d)

Lag en animasjon som viser hvordan temperaturen til legemet endrer seg over tid.

Dere trenger ikke å ta med animasjonen i rapporten, men inkluder kildekode og forklar hva dere observerer.

## Oppgave 7: Luften skal med (10 poeng)

Vår antagelse at temperaturen i luften holdes konstant ved $200$ grader er ikke realistisk. I realiteten vil temperaturen i ovnen gå ned når et kaldere legeme settes inn i ovnen. 

Det vil også oppstå konveksjon, men dette er noe vi vil ignorere i denne oppgaven.

Vi tar et lag av luft rundt legemet med i modellen. I luftlaget så har vi fortsatt den partielle differensiallikningen
$$
\begin{align}\tag{20}
u_t = \alpha \cdot ( u_{xx} + u_{yy} ),
\end{align}
$$
hvor $\alpha$ tar en annen lavere verdi som passer bedre til luft.

På randen rundt luftlaget har vi fortsatt konstant temperatur $200$ for alle verdier av $t$.

I luftlaget så er temperaturen $200$ grader når $t=0$. Temperaturen til legemet vi setter inn ved $t=0$ er $15$ grader.

Bredden og høyden av luftlaget skal være omtrent halvparten av bredden og høyden til legemet. Dere kan selv bestemme de nøyaktige dimensjonene.

### a)

Sett opp problemet og kjør beregningene fra oppgave 6 på nytt. Det vil si, sett opp den partielle differensiallikningen med rand- og initialbetingelser. Løs deretter likningen numerisk.

Legg ved et *varmeplot* (altså fargeplot med rødt for varmt og blått for kaldt) for noen ulike tidspunkt.

### b)

Lag en animasjon som viser hvordan temperaturen til legemet endrer seg over tid.

Dere trenger ikke inkludere animasjonen i rapporten, men inkluder kildekode, og diskuter hva dere observerer.

### c)

Hvor lang tid tar det før temperaturen i midten av legemet når 60 grader? Tegn et varmeplot for tidspunktet dette skjer.

## Oppgave 8 (10 poeng)

Skriv en kort oppsummering (maks en halv side) som beskriver hvordan gruppearbeidet har fungert.
